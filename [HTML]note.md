  ```
  // 设备像素
  var resolution = screen.height * screen.width;
  // 设备可用像素
  var available_resolution = screen.availHeight * screen.availWidth;
  // 屏幕的颜色深度
  var color_depth = screen.colorDepth;
  // 物理像素与设备独立像素比
  var pixel_ratio = window.devicePixelRatio;
  // 操作系统信息
  var navigator_platform = navigator.platform;
  ```
